<Page x:Class="LCTWorks.WinUI.Gallery.Views.ShellPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:c="using:LCTWorks.WinUI.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:LCTWorks.WinUI.Gallery.Views"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:models="using:LCTWorks.WinUI.Gallery.Models"
      xmlns:n="using:LCTWorks.WinUI.Navigation"
      mc:Ignorable="d">

    <Grid>
        <Grid x:Name="AppTitleBar"
              Height="{Binding ElementName=NavigationViewControl, Path=CompactPaneLength}"
              VerticalAlignment="Top"
              Canvas.ZIndex="1"
              ColumnSpacing="8"
              Loaded="AppTitleBar_Loaded"
              SizeChanged="AppTitleBar_SizeChanged">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="LeftPaddingColumn"
                                  Width="0" />
                <ColumnDefinition x:Name="IconColumn"
                                  Width="Auto" />
                <ColumnDefinition x:Name="TitleColumn"
                                  Width="Auto" />
                <ColumnDefinition x:Name="BuildTitleColumn"
                                  Width="Auto" />
                <ColumnDefinition x:Name="LeftDragColumn"
                                  Width="*" />
                <ColumnDefinition x:Name="SearchColumn"
                                  Width="4*"
                                  MinWidth="220" />
                <ColumnDefinition x:Name="RightDragColumn"
                                  Width="*"
                                  MinWidth="48" />
                <ColumnDefinition x:Name="AccountColumn"
                                  Width="Auto" />
                <ColumnDefinition x:Name="RightPaddingColumn"
                                  Width="0" />
            </Grid.ColumnDefinitions>
            <Image x:Name="TitleBarIcon"
                   Grid.Column="1"
                   Width="16"
                   Height="16"
                   HorizontalAlignment="Left"
                   Source="/Assets/WindowIcon.ico" />
            <TextBlock x:Name="AppTitleBarText"
                       Grid.Column="2"
                       VerticalAlignment="Center"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       TextWrapping="NoWrap" />
        </Grid>

        <NavigationView x:Name="NavigationViewControl"
                        Canvas.ZIndex="0"
                        DisplayModeChanged="NavigationViewControl_DisplayModeChanged"
                        IsBackButtonVisible="Collapsed"
                        IsBackEnabled="{x:Bind ViewModel.IsBackEnabled, Mode=OneWay}"
                        IsSettingsVisible="True"
                        Loaded="NavigationViewControl_Loaded"
                        MenuItemsSource="{x:Bind ViewModel.ItemsSource, Mode=OneWay}"
                        SelectedItem="{x:Bind ViewModel.Selected, Mode=TwoWay}"
                        TabIndex="0">
            <NavigationView.MenuItemTemplate>

                <DataTemplate x:DataType="models:DocItem">
                    <NavigationViewItem n:NavigationHelper.NavigateTo="{x:Bind NavigationKey}"
                                        Content="{x:Bind Title}">
                        <NavigationViewItem.Icon>
                            <ImageIcon Source="/Assets/ColorIcons/Image.svg" />
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                </DataTemplate>
            </NavigationView.MenuItemTemplate>

            <Grid Margin="0">
                <Frame x:Name="NavigationFrame" />
            </Grid>
        </NavigationView>

        <InfoBar x:Name="infoBarMessage"
                 Margin="0,64"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Top"
                 CornerRadius="16"
                 IsClosable="False" />
    </Grid>
</Page>