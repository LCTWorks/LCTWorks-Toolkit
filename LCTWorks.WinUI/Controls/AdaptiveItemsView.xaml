<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
                    xmlns:converters="using:LCTWorks.WinUI.Converters"
                    xmlns:local="using:LCTWorks.WinUI.Controls">

    <!--  Helpers  -->
    <converters:BooleanToVisibilityConverter x:Key="BoolToVis" />

    <!--  Default style for the repeater container  -->
    <Style TargetType="local:SelectableRepeaterItem">
        <Setter Property="Padding"
                Value="8,4" />
        <Setter Property="Background"
                Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:SelectableRepeaterItem">
                    <Grid>
                        <!--  Simple selection adornment; customize as needed  -->
                        <Border x:Name="SelectionBackground"
                                Background="{ThemeResource SystemControlHighlightListAccentLowBrush}"
                                CornerRadius="4"
                                Visibility="{Binding IsSelected, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVis}}" />
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Default style for AdaptiveItemsView  -->
    <Style TargetType="local:AdaptiveItemsView">
        <Setter Property="IsTabStop"
                Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:AdaptiveItemsView">
                    <Grid>
                        <ScrollViewer x:Name="PART_ScrollViewer"
                                      HorizontalScrollBarVisibility="Disabled"
                                      HorizontalScrollMode="Disabled"
                                      VerticalScrollBarVisibility="Auto"
                                      VerticalScrollMode="Auto"
                                      ZoomMode="Disabled">
                            <controls:ItemsRepeater x:Name="PART_Repeater" />
                        </ScrollViewer>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>