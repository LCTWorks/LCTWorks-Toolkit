<ContentDialog x:Class="LCTWorks.WinUI.Controls.ContentDialogs.ItemsViewContentDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:local="using:LCTWorks.WinUI.Controls.ContentDialogs"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               PrimaryButtonText="Primary"
               SecondaryButtonText="Secondary"
               Style="{StaticResource ModernContentDialogStyle}"
               mc:Ignorable="d">

    <ContentDialog.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="1"
                       HorizontalAlignment="Center"
                       MaxLines="2"
                       Text="Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore "
                       TextAlignment="Center"
                       TextWrapping="WrapWholeWords" />
        </Grid>
    </ContentDialog.Content>
    <!--<ScrollViewer Margin="{StaticResource ContentDialogLayoutRootMargin}"
                  HorizontalScrollBarVisibility="Disabled"
                  HorizontalScrollMode="Disabled"
                  IsVerticalScrollChainingEnabled="False">
        <Grid MaxWidth="{StaticResource ContentDialogMainContainerMaxWidth}"
              RowSpacing="{StaticResource ContentDialogMainContainerRowSpacing}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>

            <StackPanel Margin="{StaticResource ContentDialogContainerStackPanelMargin}"
                        Orientation="Vertical"
                        Spacing="{StaticResource ContentDialogContainerStackPanelSpacing}">

                <TextBlock HorizontalAlignment="Center"
                           FontSize="{StaticResource ContentDialogTitleFontSize}"
                           Style="{StaticResource PoppinsRegularTextBlockStyle}"
                           Text="{x:Bind Title}"
                           TextAlignment="Center"
                           TextWrapping="WrapWholeWords" />

                <Rectangle Style="{StaticResource HorizontalSeparatorRectangleStyle}" />
            </StackPanel>

            <ItemsRepeater Grid.Row="1"
                           ItemsSource="{x:Bind LinkItems}">
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="local:FeedbackLinkItem">
                        <Grid Padding="8"
                              ColumnSpacing="16"
                              RowSpacing="0">
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.RowSpan="2"
                                       FontFamily="{StaticResource FluentIconsFontFamily}"
                                       Style="{StaticResource ItemFluentIconTextBlockStyle}"
                                       Text="{x:Bind IconUri}" />
                            <TextBlock Grid.Column="1"
                                       HorizontalAlignment="Left"
                                       Style="{StaticResource ItemTitleTextBlockStyle}"
                                       Text="{x:Bind Title}" />
                            <TextBlock Grid.Row="1"
                                       Grid.Column="1"
                                       Style="{StaticResource ItemDescriptionTextBlockStyle}"
                                       Text="{x:Bind Description}" />
                        </Grid>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>

            <Rectangle Grid.Row="2"
                       Margin="32,0"
                       VerticalAlignment="Top"
                       Style="{StaticResource HorizontalSeparatorRectangleStyle}" />

            <Button Grid.Row="2"
                    Command="{x:Bind PrimaryButtonCommand}"
                    CommandParameter="{x:Bind PrimaryButtonCommandParameter}"
                    Content="{x:Bind PrimaryButtonText}"
                    Style="{StaticResource ContentDialogSoloButtonStyle}" />
        </Grid>
    </ScrollViewer>-->
</ContentDialog>